@using Microsoft.AspNetCore.Authentication
@{
    var nomeExibicao = "Visitante";
    if (User.Identity.IsAuthenticated)
    {
        nomeExibicao = @User.Identity.Name.Split(":")[0];
    }
}
@nomeExibicao